#!/bin/bash

# sterling $@ -I .. imp:composed
(cd ../.. && ./build)
echo "---------------------------------"
echo "Build completed."
echo "---------------------------------"
sterling --silver-concrete --lambda-prolog -I .. $@ imp_executable:host
